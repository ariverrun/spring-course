spring:
  datasource:
    url: jdbc:h2:mem:maindb

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        '[format_sql]': true

    show-sql: true
  
  # flyway:
  #   enabled: true
  #   baseline-on-migrate: true
  #   locations: classpath:db/migration
